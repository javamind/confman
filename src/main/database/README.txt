We use flyway to manage the database. For the development we use the gradle plugin.
Theses directories are used in the packaging to build a package to installa database.
This tool help you to install the application without any build tool or equivalent